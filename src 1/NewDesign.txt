Store selected items in a set in Navigation::CurrentState
Holds Current Folders

App
 ├── Global Services (Owned by App, passed by reference to UI)
 │    ├── TaskSystem      (Thread pool for async work)
 │    ├── IconStore       (Caches ItemId -> Icon Index)
 │    ├── TextureCache    (Caches Icon Index -> GPU ImTextureID)
 │    └── Clipboard       (Global cut/copy/paste state)
 │
 └── Tabs (std::vector<Tab>)
      ├── Tab
      │    ├── Navigation (Owns History stack, Current Path, Breadcrumbs)
      │    ├── Snapshot   (The immutable list of ItemCore for the current folder)
      │    └── ViewState  (Sort mode, ViewMode, Selection set)

Map
icon index,
extension,
type name from extension,
sidebar icons.

Map, but use the lastWriteTime as a validator, on whether to recompute
Tool tip
File metadata
File size

if lastWriteTime different, delete and recompute

folder loads,
sort changes,
hidden toggle changes,
filter/search changes,
refresh happens.

item.IconKey = GetTexture(item.hash/*next time the icon asks for it, it gets it back*/, item.pidl/* just to get the icon key*/), returns the iconKey given by windows


Get type name based on extension, but what do i do for folder, which doesnt have an extension?

struct FolderSizeJob{
    u64 itemId;
    std::vector<std::wstring> pendingDirs; // This is your "save state"
    u64 accumulatedSize = 0;
    u64 fileCount = 0;
    bool isPaused = false;
    bool isCancelled = false;
};

Note currentState has to know last clicked item